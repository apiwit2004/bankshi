name: project_name
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  url_launcher: ^6.0.20  # คุณเพิ่ม url_launcher เพื่อเปิดลิงก์
  intl: ^0.17.0  # เพิ่ม intl สำหรับจัดการวันที่
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
